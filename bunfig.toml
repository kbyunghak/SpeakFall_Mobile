[install]
saveTextLockfile = true
# 24h supply-chain guard: skip package versions published less than a day ago.
minimumReleaseAge = 86400
# Add narrowly scoped exceptions only when a newly published dependency has
# been reviewed and is required immediately.
